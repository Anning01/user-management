# ======================================
# 环境变量配置示例
# ======================================
# 说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 修改下面的配置值
# 3. 运行项目时会自动加载 .env 文件
#
# 配置优先级：
#   .env 文件 > configs/config.yaml > 默认值
#
# 重要：
# - 如果某个值留空（如 DB_PASSWORD=），表示使用空值（无密码连接）
# - 这对于本地开发的 MySQL 无密码场景很有用
# ======================================

# 服务器配置
SERVER_PORT=8080

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_password  # 如果 MySQL 无密码，请留空：DB_PASSWORD=
DB_NAME=user_management

# JWT配置（必填！）
JWT_SECRET_KEY=your-secret-key-change-this-in-production
